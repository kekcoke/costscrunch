# 2026-03-22 — LocalStack REST v1 Routing & CORS Hardening

## [Infra-P0] — API Gateway Resource Tree, Idempotent Bootstrap, Frontend Env Fix

### Added
- **Hierarchical Resource Tree**: `bootstrap.sh` now builds all 18 API Gateway resources explicitly (was flat — only top-level paths). Nested routes like `/groups/{id}/balances`, `/receipts/{expenseId}/scan`, and `/analytics/chart-data` are no longer 404-before-Lambda.
- **Scan Polling Endpoint**: `GET /receipts/{expenseId}/scan` in receipts Lambda — queries DynamoDB for Textract scan results by `RECEIPT#{expenseId}` / `SCAN#` prefix.
- **Dual Event Shape Detection**: `isApiGatewayEvent()` now detects both REST v1 (`httpMethod`) and HTTP v2 (`routeKey`) events. `resolveRoute()` normalizes both into a consistent `METHOD /path` string.

### Changed
- **Idempotent Bootstrap**: `bootstrap.sh` queries `get-rest-apis` first, creates only if no matching API exists. Previously created a new API (and ID) on every run, accumulating orphaned APIs.
- **Frontend Base URL**: `.env.dev` now requires the full REST API v1 invoke path (`/restapis/{ID}/{stage}/_user_request_`). Bare `http://localhost:4566` hit S3, not API Gateway.
- **Vite Config**: `loadEnv("test")` hardcoded mode replaced with `loadEnv(mode)` + `.env.dev` fallback. Shell env (from `dev:opt2` script) still takes priority.
- **Client Instantiation**: `receipts/index.ts` switched from raw `new S3Client({})` / `DynamoDBDocumentClient.from(new DynamoDBClient({}))` to factory functions `createS3Client()` / `createDynamoDBDocClient()` for consistent LocalStack endpoint configuration.
- **CORS Headers in Response Helpers**: `ok()` and `err()` in `receipts/index.ts` now include `Access-Control-Allow-Origin`, `Access-Control-Allow-Credentials`, and `Content-Type` headers (required for REST v1 proxy integrations — browser rejects headerless responses).
- **Route Matching**: receipts Lambda switched from exact `routeKey === "POST /receipts/upload-url"` to substring `route.includes("/upload-url")` to work with both REST v1 and HTTP v2 route formats.
- **Path Rename**: `/analytics/chartData` → `/analytics/chart-data` across backend handlers, tests, and API definitions.

### Fixed
- **Missing Closing Paren**: `receipts/index.ts` handler wrapped with `withLocalAuth(withErrorHandler(...))` but only had one closing `)` — caused esbuild build failure.
- **TypeScript TS2554 (94 errors)**: `withLocalAuth` required `context` parameter but all Lambda handlers and tests only pass `(event)`. Made `context` optional in input and output signatures.
- **Production URL Leak**: `vite.config.ts` `loadEnv("test")` loaded wrong `.env` file → `VITE_API_URL` was `undefined` → `??` fallback hit `https://api.costscrunch.io` during local dev.
- **4 Stale APIs Cleaned Up**: Deleted orphaned APIs (`2mhpmcfngl`, `9tseln10s7`, `59ki2y38fk`) accumulated from non-idempotent bootstrap.

### ⚠️ Outstanding (from earlier session)
- **Named Docker Volume**: Adding `localstack-data:/var/lib/localstack` to `docker-compose.localstack.yml` would persist the API ID across `compose down`, eliminating the need to update `.env.dev` after every restart. Not yet applied.

---

## [Infra-P0] — Test Suite Stability, CORS Origin Parity, and Mock Auth Sync

### Added
- **Vite Port in CORS**: `infrastructure/localstack/dev/setup.sh` now includes `http://localhost:5173` (Vite default) in S3 CORS allowed origins. Fixes `s3.test.ts` failures.
- **Vitest JSDOM Directives**: Added `@vitest-environment jsdom` to frontend test files. Ensures tests run in a browser-like environment regardless of global config.
- **Jest-DOM Matchers**: Explicitly imported `@testing-library/jest-dom/vitest` in test files to fix `toBeInTheDocument` ReferenceErrors.

### Changed
- **Scoped Test Queries**: Refactored `components.test.tsx` and `analytics-page.test.tsx` to use scoped queries (destructuring from `render()`) instead of the global `screen` object. Eliminated "DOM bleeding" where elements from concurrent tests interfered with assertions.
- **Mock Auth Rename**: Renamed `withLocalAuth` to `withMockAuth` in `backend/src/lambdas/_local/mockAuth.ts` to match test suite expectations and cross-backend naming conventions.
- **Mock Hoisting**: Switched to `vi.hoisted()` for API mocks in `analytics-page.test.tsx` to ensure mock instances are available during module initialization.

### Fixed
- **Ambiguous Selectors**: Updated category filter and member addition tests to use strict regex (e.g., `/^Add Member$/`) to avoid matching multiple responsive UI elements.
- **Missing API Mocks**: Added `deleteMember` and updated `getGroup` mocks in `components.test.tsx` to provide the member data expected by the UI.
- **Async State Sync**: Replaced `getByText` with `findByText` and added `vi.waitFor` blocks to correctly synchronize with React state updates and `setTimeout` transitions.

### CI/CD & Automation
- **CORS in Integration Tests**: Added `enable-cors.sh` to the `.github/workflows/workflow.yml` integration suite. Ensures the LocalStack compute layer correctly enforces CORS during headless CI runs, preventing 403 errors in the integration suite.
- **Production Smoke Test Fix**: Hardcoded the production API URL in `deploy.yml` smoke tests, replacing an unset environment variable that caused false-negative health check failures.

---

## [Infra-P0] — SAM CLI v1.155 Workaround, Opt 3 Hardening, Env Hygiene

### Root Cause Identified
- **SAM CLI v1.155 `--container-host` Bug**: Native `--container-host` networking is broken — Lambda containers fail with `socket.gaierror: [Errno 8] nodename nor servname provided, or not known` when resolving the LocalStack endpoint. This is a SAM CLI regression, not a project config issue.
- **CORS Errors Are Symptoms**: Browser CORS errors in this project were almost always caused by 5xx/502/504 infrastructure failures where API Gateway doesn't inject CORS headers. Fixing the underlying infra failure eliminates the CORS error. Chasing CORS headers alone is a red herring.

### Added
- **`awsClients.ts` — `baseConfig()` Export**: Centralized AWS SDK v3 endpoint/credential configuration. All clients now call `baseConfig()` instead of duplicating endpoint logic. Shared by `createDynamoDBDocClient()`, `createS3Client()`, and now `SESClient`.
- **SAM Version Check** (`localstack-opt3.sh` L38-47): Detects SAM CLI ≥1.155 and prints a warning about the `--container-host` bug.
- **Port Conflict Pre-check** (`localstack-opt3.sh` L49-57): Scans ports 3000 and 3001 before starting; exits with guidance if occupied.
- **`env.json` Validation** (`localstack-opt3.sh` L145-168): Uses `jq` to verify all 5 functions have `ENVIRONMENT` and `AWS_ENDPOINT_URL` before `sam build` runs. Catches empty-variable generation early.
- **Trap-based Cleanup** (`localstack-opt3.sh` L19-27): `trap cleanup EXIT INT TERM` kills background jobs (Vite dev server) on Ctrl+C or script exit.
- **Template Globals `ENVIRONMENT: dev`**: Hardcoded in `template-arm.yaml` Globals as a fallback — SAM CLI v1.155 has intermittent issues merging `env.json` overrides with template Globals in `--docker-network` mode.

### Changed
- **Networking Mode**: `localstack-opt3.sh` switched from `--container-host` to `--docker-network costscrunch-local`. Lambda containers join the Docker network and resolve LocalStack by container name (`costscrunch-localstack:4566`).
- **Template Endpoint**: `AWS_ENDPOINT_URL` in Globals changed from `http://host.docker.internal:4566` to `http://costscrunch-localstack:4566` (container-name DNS).
- **`env.json` Structure**: Fixed to use required `EnvironmentVariables` nesting per function (SAM CLI spec). Previously had flat keys which were silently ignored.
- **`env.json` Git Status**: Removed from version control and added to `.gitignore` — it's generated at runtime from `.env.dev` and contains machine-specific values.
- **`.env.dev` `VITE_API_URL`**: Removed stale hardcoded API IDs. Now defaults to `http://localhost:3001` (SAM local port). `localstack-opt3.sh` overrides at runtime via shell export.
- **Groups Lambda SESClient**: Changed from `new SESClient({})` (no endpoint config — always pointed to real AWS) to `new SESClient(baseConfig())` (respects `AWS_ENDPOINT_URL` for LocalStack).
- **Groups Lambda `normalizeRoute()`**: New path matcher that handles REST API v1 actual paths (`/groups/g1/balances`) and HTTP API v2 route keys (`GET /groups/{id}/balances`) in a single function. Replaces the old `isApiGatewayEvent()` + `resolveRoute()` dual-path approach.

### Fixed
- **`socket.gaierror: [Errno 8]`**: Eliminated by switching to `--docker-network` mode. Lambda containers now use Docker internal DNS instead of the broken `--container-host` socket resolution.
- **SESClient Pointing to Real AWS**: `groups/index.ts` instantiated `SESClient` without endpoint config, meaning local dev sent emails to real AWS (which failed silently). Now uses `baseConfig()`.
- **Silent `env.json` Misconfiguration**: SAM CLI silently ignored flat env vars (no `EnvironmentVariables` wrapper). Functions ran with missing `AWS_ENDPOINT_URL`, causing all DynamoDB calls to target real AWS. Validation step now catches this.
- **Stale API ID in `VITE_API_URL`**: Previous `.env.dev` had a hardcoded LocalStack REST API ID from a prior session. Frontend sent requests to a non-existent API. Cleaned to use SAM port directly.

### Verified
- `GET /groups` returns 200 with correct JSON body via SAM local API (port 3001).
- CORS preflight (`OPTIONS /groups`) returns correct `Access-Control-Allow-*` headers from SAM's GatewayResponses.
- Full Opt 3 stack: LocalStack (4566) → SAM local API (3001) → Vite frontend (3000).

### ⚠️ Outstanding
- **Pre-existing Lambda Logic Issues**: `/expenses` returns 500 (unrelated to CORS/infra — handler bug). `/analytics` returns 404 (handler/route mismatch). These predate this session.
- **P1-P2 Hardening**: Trap cleanup is implemented; moving Vite URL config to runtime-only is partially done (script overrides, but `.env.dev` still has a default).
- **Integration Test Coverage**: Full 18-resource API tree not yet verified under SAM local. Only `/groups` and preflight confirmed.

---
🌸 Generated with [AdaL](https://github.com/adal-cli/)
