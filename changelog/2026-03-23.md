# 2026-03-23 — DynamoDB On-Demand Audit, Test Suite Hardening

## [Infra-P1] — DynamoDB Billing Mode Verification & Unit Test Coverage

### Verified
- **MainTable & ConnTable billing**: Confirmed both tables in `CostsCrunchStack.ts` use `dynamodb.Billing.onDemand()` (`PAY_PER_REQUEST`). No changes needed — configuration was already correct for initial launch.

### Added
- **DynamoDB billing unit test** (`infrastructure/__tests__/unit/dynamodb-billing.test.ts`): CDK assertion-level tests that verify the synthesized CloudFormation template contains `BillingMode: PAY_PER_REQUEST` for both `costscrunch-dev-main` and `costscrunch-dev-connections` tables, plus a catch-all that iterates all `AWS::DynamoDB::GlobalTable` resources.

### Changed
- **Infrastructure `esbuild` dependency**: Added `esbuild` as an explicit `devDependency` in `infrastructure/package.json`. Was previously only available transitively via `vite`, causing `NodejsFunction` bundling to fail during CDK synthesis in unit tests.

---

## [Test] — Infrastructure & Backend Unit Test Fixes (5 files)

### Fixed
- **Missing `vitest` imports**: Added `describe`, `it`, `expect` (and `beforeEach`/`beforeAll` where needed) to `CostsCrunchStack.test.ts`, `EncryptionAspect.test.ts`, `dynamodb-billing.test.ts`, `analytics.unit.test.ts`, and `receipts.unit.test.ts`. Root cause: `infrastructure/vite.config.ts` sets `globals: true` but vitest must still be running through that config — direct file execution bypasses globals injection.
- **`withLocalAuth` export missing**: `backend/src/lambdas/_local/mockAuth.ts` exported `withMockAuth` but all handlers import `withLocalAuth`. Added re-export alias: `export { withMockAuth as withLocalAuth }`.
- **Floating-point precision in alarm threshold** (`CostsCrunchStack.test.ts`): Changed `Threshold: 23.2` to `Threshold: 29 * 0.8` to match IEEE 754 output (`23.200000000000003`).
- **CFN MetricFilter pattern quoting** (`CostsCrunchStack.test.ts`): Fixed `?ERROR` to `?"ERROR"` — CloudFormation escapes filter pattern terms with double quotes.
- **Stale alarm resource count** (`CostsCrunchStack.test.ts`): Updated from 20 to 21 to account for the DLQ message count alarm.
- **Missing env var in receipts test** (`receipts.unit.test.ts`): `BUCKET_UPLOADS_NAME` was read at module load time but not set, causing `createPresignedPost` to receive `Bucket: undefined`. Added fallback assignment.

### Removed
- **Duplicate billing test file**: Merged `dynamodb.billing.test.ts` into `dynamodb-billing.test.ts` (3 consolidated test cases). Deleted the redundant file.

---
🌸 Generated with [AdaL](https://github.com/adal-cli/)
