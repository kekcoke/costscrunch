# 2026-03-24 — WAF v2 Hardening: SQLi Rule, Path-Specific Rate Limiting, Access Logging

## [Infra-P1] — WAF v2 WebACL Enhancement (5 rules + logging)

### Added
- **AWSManagedRulesSQLiRuleSet** (priority 2): SQL injection detection managed rule group, inserted between CommonRuleSet and KnownBadInputsRuleSet.
- **Path-specific rate limiting** (priority 5): 200 requests/5-minute window per IP, scoped to `POST /receipts/upload` via `AndStatement` combining `ByteMatchStatement` on method (`POST`) and URI path (`/receipts/upload`, `EXACTLY` positional constraint). Prevents abuse of the receipt upload presigned-URL endpoint without affecting general API traffic.
- **WAF access logging**: Dedicated CloudWatch LogGroup (`/aws/wafv2/{prefix}-waf-logs`) with 90-day retention. Implemented as a separate `AWS::WAFv2::LoggingConfiguration` resource (CDK 2.241+ requirement — not a property on `CfnWebACL`). Authorization header redacted from logs to prevent Cognito token leakage.
- **WAF LogGroup**: 90-day retention, environment-aware removal policy (RETAIN in prod, DESTROY otherwise).

### Changed
- **Global rate limit**: Increased from 2,000 to 5,000 requests/5-minute window per IP to reduce false positives at scale.
- **Rule priorities**: Reordered to accommodate new SQLi rule — KnownBadInputs shifted to priority 3, global rate limit to 4, receipt upload rate limit to 5.
- **WebACL description**: Added descriptive text (`"CloudFront WAF: managed rules + rate limiting"`).

### Cost Impact
- +$1/month (additional rule: SQLi)
- +$5–20/month (WAF access logging ingestion, traffic-dependent)
- +$0.50–2/month (CloudWatch log storage at 90-day retention)
- **Total delta: ~$7–19/month** (from ~$8 to ~$15–27)

### Notes
- CloudFront-scoped WebACL logs must be delivered to **us-east-1**. If this stack is deployed to another region, the LogGroup must be moved to a separate us-east-1 stack and referenced cross-stack (noted in-code).
- `CfnLoggingConfiguration` is a standalone CloudFormation resource in CDK 2.241+ — it cannot be set as a property on `CfnWebACL`. This was discovered during implementation and test debugging.

---

## [Test] — WAF CDK Assertion Tests (6 new tests)

### Added
- **`CostsCrunchStack.test.ts` — "CostsCrunchStack WAF" describe block**: 6 tests using CDK `Match` helpers (not vitest matchers, which are incompatible with `hasResourceProperties`):
  1. CloudFront-scoped WebACL with exactly 5 rules at correct priorities
  2. SQLi managed rule group presence and vendor/name
  3. Global rate limit at 5,000 per IP with Block action
  4. Path-specific rate limit scoped to POST `/receipts/upload` at 200 per IP, including TextTransformations in ByteMatchStatement assertions
  5. WAF LogGroup with 90-day retention + CfnLoggingConfiguration with Authorization header redaction
  6. WebACL attachment to CloudFront distribution (verifies `Fn::GetAtt` reference)

### Fixed
- **Snapshot**: Auto-updated to reflect new WAF template shape (additional rule, logging configuration resource, log group).

---

## [AppSec-P2] — Secrets Management: SSM Parameter Store + Secrets Manager

### Added
- **SSM Parameter Store** for configuration values:
  - `/costscrunch-{env}/bedrock-model-id` — Bedrock Claude model ID
  - `/costscrunch-{env}/vite-app-url` — Frontend URL for CORS
  - `/costscrunch-{env}/from-email` — SES sender email
  - `/costscrunch-{env}/pinpoint-app-id` — AWS Pinpoint application ID

- **Secrets Manager** for sensitive notification config:
  - `costscrunch-{env}/notification-config` — JSON secret containing `fromEmail` + `pinpointAppId`

- **Lambda environment variables** now store parameter paths instead of actual values:
  - `SSM_BEDROCK_MODEL_ID` — SSM parameter path for Bedrock model
  - `SSM_VITE_APP_URL` — SSM parameter path for frontend URL
  - `SECRET_NOTIFICATION_ARN` — Secrets Manager ARN for notification config

- **IAM permissions** granted per least-privilege:
  - `ssm:GetParameter` for Bedrock model parameter
  - `secretsmanager:GetSecretValue` for notification secrets

- **CDK Security Aspect** (`SecretsGuardAspect`): Fails synthesis if sensitive values are added as plain-text Lambda environment variables. Blocks patterns including:
  - Keys matching `api_key`, `secret`, `password`, `token`, `credential`, `bearer`
  - AWS access key patterns (`AKIA...`)
  - Email addresses (`@costscrunch.com/io/dev`)

### Added
- **`SecretsAudit.test.ts`**: 8 tests asserting:
  1. No hardcoded email addresses in Lambda env vars
  2. No hardcoded sensitive values (API keys, passwords, tokens)
  3. No secrets exposed in CloudFormation Outputs
  4. Notification secrets stored in Secrets Manager
  5. Configuration stored in SSM Parameter Store
  6. Lambda functions use SSM/SECRET path references (not values)
  7. Notifications Lambda has Secrets Manager read access
  8. `FROM_EMAIL` and `PINPOINT_APP_ID` not stored as plain env vars

### Removed
- **Hardcoded secrets** from `sharedEnv` object:
  - `BEDROCK_MODEL_ID` (hardcoded fallback)
  - `VITE_APP_URL` (hardcoded fallback)
  - `FROM_EMAIL` (was hardcoded on `notificationsLambda`)
  - `PINPOINT_APP_ID` (was hardcoded on `notificationsLambda`)

### Security Impact
| Control | Before | After |
|---------|--------|-------|
| Secrets in env vars | ✅ Sensitive values exposed | ❌ Paths only |
| CloudFormation outputs | ✅ No secrets | ✅ No secrets |
| CDK synthesis guard | ❌ None | ✅ Blocks sensitive env vars |
| Runtime secret retrieval | N/A | ✅ SSM + Secrets Manager |

### Notes
- Lambdas must be updated to retrieve secrets at runtime via AWS SDK (`SSMClient`, `SecretsManagerClient`)
- SSM parameters support runtime updates without Lambda redeployment
- VPC endpoints for SSM and Secrets Manager already provisioned in stack

---

## [Infra-P3] — AWS X-Ray Tracing on Receipt Pipeline

### Added
- **X-Ray Active tracing** on all receipt-pipeline Lambda functions: `receipts`, `image-preprocess`, `sns-webhook`
- **1% sampling rate** derived from environment variable (`TRACE_SAMPLE_RATE=0.01`) — balances observability with cost
- **Custom subsegments** in sns-webhook handler with stage annotations:
  - `textract` — wraps `GetExpenseAnalysis` call
  - `categorize` — wraps Bedrock `InvokeModel` call
  - `preprocess` — wraps Sharp image compression
- **Trace annotation**: `receiptId` captured on every trace for CloudWatch filter-based debugging
- **IAM permissions**: `xray:PutTraceSegments` added to receipt-pipeline Lambda execution roles

### Cost Impact
- ~$0.50/month (X-Ray trace ingestion at 1% sampling on low-volume pipeline)

---

## [Bugfix] — Backend Handler & Test Fixes

### Fixed
- **GroupsFunction use-before-declaration**: Logger called before instantiation, causing 500 errors in LocalStack. Reordered declarations so `logger` exists before first use.
- **Expenses handler duplicate key**: Removed duplicate `Limit` property in DynamoDB query params that caused validation errors.
- **sns-webhook Tracer mock incomplete**: Added missing `putAnnotation` and `addAnnotation` methods to inline Tracer mock, resolving 3 unit test failures.
- **Integration test API discovery**: Removed stale `API_ID` from `.env.dev`; implemented dynamic `docker exec` fallback for API Gateway ID discovery. LocalStack v3.x does not reliably persist API state via volume mounts — dynamic discovery via `awslocal apigateway get-rest-apis` is now the standard approach.

### Test Results
- 159 unit tests passing
- 54 integration tests passing

---

## [SRE-P2] — Circuit Breaker for Textract & Bedrock Calls

### Added
- **`backend/src/utils/circuitBreaker.ts`**: State-machine circuit breaker (CLOSED → OPEN after 5 failures → HALF-OPEN after 30s cooldown).
- **Integration in sns-webhook**: Textract `GetExpenseAnalysis` and Bedrock `InvokeModel` calls wrapped with per-service breakers.
- **Graceful degradation**: On Textract circuit open → scan status set to `pending_manual_review` (no Textract call made). On Bedrock circuit open → falls through to keyword-based `guessCategory()` fallback.
- **Unit tests**: 10 tests covering state transitions, cooldown timing, success reset, and half-open behavior.

### Cost Impact
- None (pure in-memory state, no additional AWS resources).

---

## [Data-P1] — DynamoDB Transactional Writes with Retry

### Added
- **`backend/src/utils/transactWriteWithRetry.ts`**: Exponential-backoff retry wrapper for `TransactWriteCommand`. Retries only on `TransactionConflict` (transient); `ConditionalCheckFailed` (business logic) propagates immediately. 3 retries, 100/200/400ms backoff.
- **sns-webhook `writeScanCompleted`**: Converted from sequential `UpdateCommand` calls to atomic `TransactWriteItems` — scan status update + expense back-fill in one transaction. `ConditionExpression` on `#status = :processing` prevents duplicate completion from replayed SNS messages.
- **Groups handler**: All three multi-item writes converted to `TransactWriteItems`:
  - **Create group**: Group profile + owner membership (atomic, `attribute_not_exists` guard).
  - **Add member**: Group profile `list_append` + member record (atomic).
  - **Remove member**: Group profile `REMOVE` by index + member record delete (atomic, index-based `ConditionExpression` prevents stale-index writes).
- **409 Conflict responses**: Groups handler catches `TransactionCanceledException` with `ConditionalCheckFailed` and returns 409 for duplicate group names and concurrent member modifications.

### Changed
- **sns-webhook**: `TransactWriteCommand` import removed from handler (constructed internally by `transactWriteWithRetry`).
- **groups**: `TransactWriteCommand` import removed; all three transaction calls use `transactWriteWithRetry(ddb, ...)`.

### Fixed
- **`transactWriteWithRetry` return type**: Replaced `ReturnType<DynamoDBDocumentClient["send"]>` (unresolvable generic overload) with concrete `TransactWriteCommandOutput`.
- **sns-webhook unit tests**: Updated assertion helpers (`findTransactItemByStatus`, `findTransactItemByPkPrefix`) to match `TransactWriteItems` input shape instead of `UpdateCommand`.

### Test Results
- 198 unit tests passing (13 files)
- Integration tests: no changes needed (assert final DDB state, not command types)

### Data Integrity Impact
| Operation | Before | After |
|-----------|--------|-------|
| Scan completion | Sequential updates (partial failure possible) | Atomic transaction (all-or-nothing) |
| Group creation | Sequential puts | Atomic with duplicate-name guard |
| Member add/remove | Sequential writes (race condition possible) | Atomic with conditional checks + retry |
| Duplicate SNS replay | Could overwrite completed scan | Silently ignored (condition check) |

---
🌸 Generated with [AdaL](https://github.com/adal-cli/)

## [Backend] — Zod Input Validation Across API Routes

### Added
- **Centralized Zod schemas** (`backend/src/shared/validation/schemas.ts`): Unified input validation for all API routes, replacing scattered manual `if` checks and type casting.
- **6 new schemas** covering all user-controlled inputs:
  - `createExpenseSchema` — POST /expenses body
  - `updateExpenseSchema` — PATCH /expenses body (merchant + dynamic field mapping)
  - `getExpensesQuerySchema` — GET /expenses query params
  - `createGroupSchema`, `updateGroupSchema`, `addGroupMemberSchema` — group routes
  - `initiateUploadSchema` — POST /receipts/upload-url
  - `analyticsQuerySchema` — GET /analytics query params
- **`validate()` helper function**: Returns `ValidationResult<T>` with structured success/error.

### Fixed
- **`expenseStatusSchema`**: Added missing `"pending"` status (pre-existing bug — `filters by status=pending` was failing).
- **`initiateUploadSchema`**: Renamed `mimeType` → `contentType` to match existing frontend contract.

### Changed
- **4 handlers refactored**: `expenses/`, `groups/`, `receipts/`, `analytics/` — all POST/PATCH routes use `schema.safeParse()`.
- **Unit test assertions updated**: Error regex patterns updated to match Zod's error format.
- **`approvalRequired` default corrected**: `false` → `true` in `createGroupSchema`.

### Security Impact
- Input validation enforced at Lambda handler layer, not just frontend
- Structured error messages prevent information leakage
- Schema `.strict()` on create operations rejects unknown fields

### Notes
- EventBridge/SNS handlers (`notifications`, `sns-webhook`, `ws-notifier`) and S3 handlers (`image-preprocess`) process internal AWS event structures — no user input to validate
- `health/index.ts` is read-only — no validation needed
- Handler-side limit capping preserves `caps limit at 200` test contract

---

