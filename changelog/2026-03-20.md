# 2026-03-20 — KMS Rotation Enforcement & Stack Refactoring

## [AppSec-P1] — KMS Key Rotation & Unit Test Stability

### Added
- **Infrastructure Security**: Enforced `enableKeyRotation: true` on the primary KMS key in `CostsCrunchStack.ts`.
- **Centralized Configuration**: Introduced `infrastructure/stacks/StackConfig.ts` and `buildStackConfig()` to manage environment variables and dependency injection for the CDK stack.
- **Test Environment Support**: Added `infrastructure/.env.test` and updated `infrastructure/__tests__/setup.ts` to provide stable dummy values during Vitest execution.

### Fixed
- **CDK Token Resolution**: Resolved `Unrecognized token key` errors in unit tests by removing direct string manipulation (`.replace()`, `.split()`, `.includes()`) on CDK Tokens.
- **Stack Synthesis**: Fixed a truncation error in `CostsCrunchStack.ts` that caused "Unterminated string literal" failures during synthesis.
- **VPC Endpoints**: Refactored VPC Interface Endpoint creation to use static identifiers instead of regex-transformed service names.

### Changed
- **Dependency Injection**: `CostsCrunchStack` now accepts an optional `config` property in its props, enabling deterministic testing without relying on `process.env` during synthesis.
- **Vitest Configuration**: Updated `infrastructure/vite.config.ts` to correctly handle the new environment-aware stack architecture.

---
🌸 Generated with [AdaL](https://github.com/adal-cli/)
