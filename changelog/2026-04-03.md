# Changelog - 2026-04-03

## 🚀 Features & Improvements
- **Authentication & Authorization**:
    - Added `POST /groups/:id/join` endpoint for accepting group invitations.
    - Added `DELETE /groups/:id` endpoint for group removal with business rule validation.
    - Added "Delete Group" support to SAM ARM templates for local development parity.
    - Enhanced group route checks and simplified authorization logic.
- **Expenses**:
    - Improved `DELETE /expenses/:id` reliability by wrapping in try-catch and verifying PK existence before deletion.
    - Prioritized `routeKey` in expenses handler to ensure consistent routing during integration tests.

## 🛠️ Fixes & Refactoring
- **Backend Stability**:
    - Resolved multiple unit test regressions and TypeScript errors across expenses and groups handlers.
    - Fixed inverted balance assertions in `calculateBalances` tests.
    - Corrected test data where owners were incorrectly listed in splits.
- **Testing Infrastructure**:
    - Added missing `createS3Client` mocks.
    - Rewrote mocks to use global references, ensuring consistency across hoisted `vi.mock` calls.
    - Fixed `POST` test bodies and PK assertions in unit tests.
    - Added specialized mocks for PDF export testing.
    - Updated error mocking to use the `error.name` property for better diagnostic accuracy.
